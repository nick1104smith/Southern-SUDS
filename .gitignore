.claude/
wordpress-theme/
.DS_Store
Thumbs.db

# CLI tooling artifacts only — not source, nothing to track.
# supabase/functions/ is a duplicate of supabase/edge-functions/ content in
# the layout the Supabase CLI expects for deploy; the real source of truth
# is supabase/edge-functions/.
supabase/functions/
supabase/.temp/
